{
  "name": "site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build-sass && browser-sync start --server --files src/css/style.css",
    "build-sass": "sass --source-map=none --style compressed --no-charset src/scss/style.scss src/css/style.css",
    "watch-sass": "sass src/scss/style.scss src/css/style.css -w"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "sass": "^1.85.1"
  },
  "dependencies": {
    "outdated": "^0.1.1"
  }
}
